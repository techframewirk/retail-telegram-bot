
const handleBooking = async (cachedData, data) => {
    switch(cachedData.nextStep) {
        case 'pickupLocation':
            console.log('Pickup')
            break
    }

}

module.exports = handleBooking